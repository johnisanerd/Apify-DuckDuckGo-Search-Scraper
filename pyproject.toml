[project]
name = "apify-duckduckgo-search-scraper"
version = "0.1.0"
description = "Example client for the DuckDuckGo Search Scraper Apify Actor"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "apify-client>=1.4.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
